#!/bin/sh

BINDIR=`dirname $0`
. $BINDIR/config

JAVABLUE_HOME=$BINDIR/.. 
CLASSROOT=$JAVABLUE_HOME/classes

CLASSPATH=`makepath "$CLASSROOT":$CLASSPATH`
export CLASSPATH

# Run it
java jdepend.JDepend $@
