$PKGLINE
/**
 * Décrivez votre interface $CLASSNAME ici.
 * 
 * @author  (votre nom) 
 * @version (un numéro de version ou une date)
 */

public interface $CLASSNAME
{
	/**
	 * Exemple d'entęte de méthode - remplacez ce commentaire par le vôtre
	 * 
	 * @param  y	le paramčtre de cette méthode
	 * @return		le résulta retourné par sampleMethod 
	 */
	int sampleMethod(int y);
}
