$PKGLINE
/**
 * Décrivez votre classe abstraite $CLASSNAME ici.
 *
 * @author  (votre nom)
 * @version (un numéro de version ou une date)
 */
public abstract class $CLASSNAME
{
	// variables d'instance - remplacez cet exemple par le vôtre
	int x;

	/**
	 * Un exemple de méthode concrčte
	 * 
	 * @param  y	le paramčtre de cette méthode
	 * @return		la somme de x et de y
	 */
	public int sampleMethod(int y)
	{
		// Insérez votre code ici
		return x + y;
	}

	/**
	 * Un exemple de méthode abstraite
	 * @param  y	le paramčtre de cette méthode
	 * @return		qui sait ?
	 */
	abstract public int sampleAbstractMethod(int y)
}
