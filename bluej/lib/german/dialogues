compile-while-executing
Klassen können nicht übersetzt werden
während ein Programm ausgeführt wird.
Das Programm muss erst beendet werden.

exception-thrown
Eine Exception ist ausgelöst worden:

break-no-source
Ein Breakpoint wurde erreicht in einer Datei,
für die der Quelltext nicht gefunden werden
kann. Name der Datei:

error-in-file
Ein Fehler ist aufgetreten in einer Datei,
für die der Quelltext nicht gefunden werden
kann. Name der Datei:Zeilennummer:

system-exit
Der Aufruf ist durch eine explizite "exit"-
Operation beendet worden. Ein Ergebnis wurde
nicht zurückgegeben. Der "exit" code ist:

no-help
Für diese Fehlermeldung ist kein Hilfetext
verfügbar. Bitte schicken Sie den Text der
Fehlermeldung an mik@monash.edu.au -
wir werden dann einen Hilfstext hinzufügen.

# errors:
#########

no-lib-selected
<Dieser Hilfstext ist noch nicht übersetzt worden.>
You have not specified a library to go to.
Click on a library in the "Search results" list
and then press the "Go to" button.

no-target-dialog
<Dieser Hilfstext ist noch nicht übersetzt worden.>
Before you use a package from
the Library Browser, you need
at least one target package open
in a BlueJ window.

cannot-locate-lib-node
<Dieser Hilfstext ist noch nicht übersetzt worden.>
Cannot locate the selected node in the
LibraryChooser tree.  The usual cause of
this error is an out-of-date package file
in the directory containing the package.

missing-shadow
<Dieser Hilfstext ist noch nicht übersetzt worden.>
Cannot locate shadow area:

cannot-run-compiler
Der Compiler konnte nicht gestartet werden.
Ist er im Kommandopfad?
Name des Compilers:

compiler-error
Compilerfehler:

stream-incomplete
Compilerfehler. Der "error stream"
ist unvollständig.

out-of-bounds
Der angegebene Array-Index liegt
außerhalb der Array-Grenzen.

cannot-access-element
Das angegebene Arrayelement konnte
nicht gelesen werden.

appletviewer-error
Bei dem Versuch, den Appletviewer zu
starten, ist ein Fehler aufgetreten.

error-no-name
Ein gültiger Name muss angegeben werden.

error-save-file
Fehler beim Versuch, die Datei zu speichern.
Die Datei wurde nicht gespeichert.

cannot-save-keys
Beim Versuch, die Tastenbindungen zu
speichern, ist ein Fehler aufgetreten.
Die Tastenbindungen konnten nicht
gespeichert werden.

error-no-templates
The specified template directory (the directory where 
BlueJ finds the class templates) does not exist. It
is defined with the property "bluej.templatePath" in
<bluej-home>/lib/bluej/defs. Check and fix this property.

skeleton-error
Das Skelett für die Klasse konnte nicht erzeugt
werden. Mögliche Gründe dafür sind eine
inkorrekte Installation oder Probleme, das
Dateisystem zu lesen.

error-open-source
Die Quelltextdatei der Klasse konnte nicht
geöffnet werden. Das sieht böse aus!

error-open-readme
Die Textnotiz für dieses Projekt konnte
nicht geöffnet werden. Eine neue, leere
Notiz wurde erzeugt.

error-writing-readme
Die README-Datei dieses Projektes konnte 
nicht erweitert werdem um die Information
der "main class" hinzuzufügen.

error-exporting
Beim Versuch, die "Export"-Funktion auszuführen,
ist ein Fehler aufgetreten. Einige oder alle
Dateien sind möglicherweise nicht kopiert
worden.

error-writing-jar
Die "jar"-Datei konnte nicht geschrieben 
werden. (Zugriffsrechte fehlen? Dateisystem
voll?)

invalid-class-name
Ungültiger Klassenname.

invalid-package-name
Invalid Package Name.

directory-exists
Ein Verzeichnis mit diesem Namen
existiert bereits.

cannot-copy-package
Es gab Probleme beim Kopieren der
Package-Dateien. Das Package konnte
nicht oder nur teilweise geschrieben
werden.

file-does-not-exist
Die angegebene Datei existiert nicht.

cannot-import
Die Datei kann nicht importiert werden.
Nur Java-Quelltextdateien, deren Namen
mit ".java" enden, können importiert
werden.

docdir-blocked-by-file
Das Verzeichnis für die Dokumentation konnte
nicht angelegt werden, weil im Projektverzeichnis
bereits eine Datei gleichen Namens existiert.

docdir-not-created
Das Verzeichnis für die Dokumentation konnte
nicht im Projektverzeichnis angelegt werden.

no-permission-for-docdir
Das Verzeichnis für die Dokumentation konnte
aufgrund mangelnder Zugriffsrechte nicht im
Projektverzeichnis angelegt werden.

doctool-error
Javadoc hat einen Fehler gemeldet. Mögliche 
Gründe sind:
  - ein Syntaxfehler in einer Klasse
  - die URL zur Javadokumentation (in den
    Einstellungen) is inkorrekt
Abhilfe: Alle Klassen übersetzen und prüfen,
dass die URL für die Dokumentation korrekt
und erreichbar (oder abgeschaltet) ist. Falls
der Fehler wieder auftaucht, bitte das Javadoc
Logfile lesen:

severe-doc-trouble
Während der Generierung der Dokumentation
ist eine Exception aufgetreten.

no-java-sources-found
Es wurden keine Java-Quelltextdateien
in diesem Verzeichnis gefunden.

duplicate-name
Eine Klasse mit diesem Namen gibt es
in diesem Package schon. Das Package
kann nicht zwei Klassen mit gleichem
Namen enthalten.

error-in-import
Bei dem Versuch, die Datei zu importieren,
ist ein Fehler aufgetreten. Prüfen Sie
Zugriffsrechte und stellen Sie sicher,
dass Speicherplatz verfügbar ist.

no-class-selected
Es ist keine Klasse selektiert.
Selektieren Sie erst eine Klasse
(durch draufklicken).

really-remove-class
Das Löschen der Klasse kann nicht
rückgängig gemacht werden.
Wirklich löschen?
Löschen
Abbrechen
null

really-remove-package
Removing this package will permanently
delete the package directory (including ALL
the contents of that directory).
Do you want to continue?
Remove
Cancel
null

remove-package-open
The package you have selected for
deletion has some sub-packages opened
within BlueJ. You must close these
packages before you will be allowed
to delete this package.

package-does-not-exist
Das Package konnte nicht geöffnet werden -
es scheint nicht zu existieren:

no-bluej-package
Das Package konnte nicht geöffnet werden.
Es ist kein BlueJ package:

applet-height-width
Höhe und Breite des Applets
müssen angegeben werden.

cannot-read-help
Die folgende Hilfsdatei konnte
nicht gelesen werden:

not-yet-implemented
Noch nicht implementiert - sorry.

quit-all
Alle offenen Packages schließen?
Alle schließen
Abbrechen
null

really-reload
Durch nochmaliges Laden gehen alle Änderungen
seid dem letzten Speichern verloren. Wirklich
laden?
Laden
Abbrechen
null

really-print
Der Text ist breiter als das Papier. Lange
Zeilen werden abgeschnitten werden. Dies kann
vermieden werden, wenn das Editorfenster schmaler
gemacht wird. Soll trotzdem gedruckt werden?
Drucken
Abbrechen
null

open-non-bluej-already-bluej
The directory you have selected is already
a BlueJ project.

open-non-bluej-invalid
You have indicated you wish to open this
directory as a BlueJ project, however a file
has been found with a package statement that
is incompatible with the current directory
structure. (For example, if the Java class
Shape has a package line of com.aa, then the
file Shape.java must be in a directory aa
which is itself in a directory com. This com
directory in turn must be in another directory
which is the directory you should point BlueJ
to when attempting to open it).

import-into-current
You currently have a package open in this window.
Do you want to import into the current package
or would you like to create a new project and
import into that?
Current
New Project
Cancel

package-name-invalid
You have changed the package statement which
to a package which does not exist in this project.
If you wish to move this class to another
package you must create the destination
package first. The package statement has been
reverted back to its original form.

package-name-changed
You have changed the package statement which
tells Java the package this class is in. Do you
want to move this class to the indicated package
or do you want to leave the class where it is
(the package statement will be reverted back to
its original form)?
Move
Revert
null

ask-index
Index des Arrayelementes [...]
Arrayinspektion
null

classmgr-changes-no-effect
The changes you have made to the class libraries
will not take effect until the next time you
start BlueJ.

# editor:
#########

no-search-string
Es ist kein Suchtext definiert.
(Zuerst Text selektieren oder 'Suchen' benutzen.)

default-keys
Sollen wirklich alle definierten
Tastenbindungen gelöscht werden und
Standarddefinitionen gesetzt werden?
Standard setzen
Abbrechen
null
