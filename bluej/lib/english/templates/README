This directory contains some templates used by BlueJ to generate files.
These templates may be edited to change the default content of generated
files.

 - The readme template may be freely edited.
 - The method template should edited with care. It is used when using the
   editor menu to insert a new method.

The subdirectory "newclass" contains templates for classes generated
via the "New Class" function in BlueJ.
