compile-while-executing
虚拟机运行时不能编译，否则会产生异常情况!


exception-thrown
产生例外:

break-no-source
设置断点时发现断点所在文件不存在. 文件名:

error-in-file
源文件没找到由此产生错误.
类:行号:

system-exit
通过一个显式的"exit"指令结束此方法. 无返回结果.
exit代码是:

no-help
此信息无帮助文件.
请将出错信息文件用邮件发至
mik@monash.edu.au  
我们将在帮助数据库中添加此帮助文档
谢谢

# errors:
#########

no-lib-selected
你没有指定一个库.
点击选中"查找结果" 列表中的一个库
并且按"Go to"按钮.

no-target-dialog
在你从库浏览器中使用一个包之前，
你应该首先在BlueJ窗口中至少打开一个目标包.

cannot-locate-lib-node
不能在库选择树定位中定位所选择的节点.
引起此错误的原因通常是包文件过期.


missing-shadow
无法定位阴影区:

cannot-run-compiler
不能运行编译器.该程序被添加到path中了吗?
编译器的名字:

compiler-error
编译错误:

stream-incomplete
编译错误. 错误流不完整.

out-of-bounds
数组元素越界.

cannot-access-element
不能访问数组元素.

appletviewer-error
在小应用程序阅览器中运行applet出错.

error-no-name
你必须指定一个合法的名字.

error-save-file
试图保存文件时产生错误
文件没有被保存.

cannot-save-keys
当试图保存热键设置时出错.
热键设置没有被保存.

error-no-templates
指定的模版目录（即BlueJ找到类模版的目录）不存在。
它一般定义在<bluej-home>/lib/bluej/defs属性文件的"bluej.templatePath"
属性中，请仔细检查并改正该属性。

skeleton-error
缺省的类框架不能产生. 
这可能是由于安装BlueJ时配置错误
或者是系统访问权限问题.

error-open-source
无法打开类的源文件.

error-open-readme
打开此项目的文本时产生错误.
建立了一个空的新文本.
 
error-writing-readme
无法增加主类信息，此项目的README文件更新失败，
更改文件时产生错误.

error-exporting
当试图导出 Application 时产生错误.
部分或全部文件无法正确复制.

error-writing-jar
jar文件不能被正确地写.
(检查文件系统存取权限和可用磁盘空间.)

invalid-class-name
不合法的类名.

invalid-package-name
不合法的包名.

directory-exists
此文件或目录名已存在.

cannot-copy-package
复制包文件时产生错误.包没被保存或保存不完整.

file-does-not-exist
指定的文件不存在.

cannot-import
不能导入文件. 文件必须是Java源文件
(它名字后缀必须为".java").

docdir-blocked-by-file
文档目录无法建在项目目录中，因为有同名文件。

docdir-not-created
文档目录无法建在项目目录中。

no-permission-for-docdir
文档目录无法建在项目目录中，因为权限不够。

show-or-generate
你是否想显示以前生成的文档，或者重新生成所有的文档？
显示
重新生成
取消

doctool-error
Javadoc 报告出错。
出错原因是在你准备产生文档的其中一个文件中存在
语法错误。也有可能是参数设置中的 JDK 文档 URL 不正确。
请确认你所有的Java文件都编译通过且文档 URL 可以访问。
再试一次,如果此错误依然存在，就应该检查 Javadoc 产生
的错误信息的日志文件:

severe-doc-trouble
调用外部文档工具时抛出例外。

no-java-sources-found
此目录下无Java源文件。
 
duplicate-name
包中已存在于此同名的类文件，同一个包中不能有同名的类文件。

error-in-import
导入文件时出错。检查存取权限和磁盘空间。

no-class-selected
没有选择可删除的类或包。删除前请点击选择类或包。 

really-remove-class
删除此类将永久删除源文件，是否继续？
删除
取消
无

really-remove-package
删除此包将永久删除包目录（包括此目录下的所有
内容），是否继续？
删除
取消
无

remove-package-open
所选中删除的包文件的子包被BlueJ打开。
必须关闭这些子包才可以删除此包。

package-does-not-exist
打开包时出错。
包不存在：

no-bluej-package
打开包时出错。
文件或目录不是BlueJ的包：

applet-height-width
必须指定 Applet 的高和宽。

cannot-read-help
无法读取帮助文件:

not-yet-implemented
仍未执行 - 抱歉.

quit-all
退出所有的打开项目?
所有的均退出
取消
无

really-reload
从最后一次编辑中恢复所有的丢弃的改变文档，确定吗？
重新恢复
取消
无

really-print
文本比打印纸宽. 宽出的部分将被裁减，为了避免被裁减，
请更改编辑器窗口尺寸使之变窄。你想就这样打印吗？
打印
取消
无

open-non-bluej-already-bluej
所选择的目录已是BlueJ 项目。

open-non-bluej-invalid
必须指明欲打开的目录是BlueJ项目。
可是，所发现的包中文件与当前目录结构是矛盾的。
（例如：如果Shap类文件中有一行包的信息com.aa，则
Shape.java必须在aa目录中，且这个目录必须在com目录中，
此com目录必须在另一个你打开的BlueJ目录中。）

import-into-current
在当前窗口中打开一个包.是否导入到此包中
或新建一个项目并且导入到新的项目中？
当前
新建一个项目
取消

package-name-invalid
你将包的描述改变为一个此项目中不存在的包。
如果你想把此类移到另一个包中，你必须首先
保证目标包存在。包的描述被重设为原来的描述。

package-name-changed
你改变了包的描述，告诉Java这个类所在的包。
是否将此类移到指定的包中或者保持不变（包的
描述将重新设置为原始的描述）？
移走
回复
无

ask-index
输入数组元素下标[...]
查看数组
无

classmgr-changes-no-effect
对类库的改变只有重新启动BlueJ才会生效.

# editor:
#########

no-search-string
没有定义查找串.
(首先选中欲查找的串或者使用'查找'.)

default-keys
你是否真的想丢弃所有的热件设置并且选择缺省的设置?
设置为缺省
取消
无
